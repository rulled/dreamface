{
  "name": "dreamface-batch-assistant",
  "version": "1.3.1",
  "private": true,
  "type": "module",
  "description": "Node test harness for the DreamFace Batch Assistant extension scripts (no runtime dependencies).",
  "scripts": {
    "test": "node --test tests/*.test.mjs"
  }
}
